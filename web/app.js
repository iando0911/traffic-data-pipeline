/*
 * app.js — web/app.js
 *
 * 注意：v2.3 起，index.html 已將圖表初始化與資料載入邏輯內嵌於 <script>，
 * 本檔案保留為「向後相容備用」，不再是主要執行路徑。
 *
 * 若未來需要把邏輯抽回獨立檔案，可將 index.html 內的 <script> 區塊移回此處，
 * 並在 index.html 改回 <script src="./app.js"></script>。
 */

// 此檔案目前為空白佔位，實際邏輯已移入 index.html 的 inline <script>
// 保留此檔案避免部署時因找不到 app.js 而產生 404 錯誤（若舊版連結仍存在）
